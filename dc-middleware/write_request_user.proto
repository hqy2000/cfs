syntax = "proto3";
package write_request_user

service WriteUser {
  // Send a write request
  rpc Write (WriteRequestUser) returns (WriteReplyUser) {}
}

message WriteRequestUser {
  bytes data = 1;
  bytes signature = 2;
}

message WriteReplyUser {
  bytes pathHash = 1;
}